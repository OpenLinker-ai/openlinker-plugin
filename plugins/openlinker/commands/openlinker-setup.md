---
description: Verify or install the OpenLinker CLI required by this plugin
---

Use the bundled `setup-openlinker-cli` skill. Verify the existing CLI first and
install only the exact checksum-pinned release after showing the source,
version, platform, and destination. Do not expose credentials or fall back to
remote MCP.
