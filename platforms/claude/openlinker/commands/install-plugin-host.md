---
description: Install the pinned native Plugin host without Go or OpenLinker CLI
---

Use the bundled `setup-plugin-host` Skill. This command explicitly requests host
installation. Show the installer plan, install the locked current-platform release,
verify it, and reload plugins. Do not enable Agent Mode or expose credentials.
