---
description: Configure, diagnose, enable, or disable Claude Code as an OpenLinker Agent
argument-hint: [request]
---

Use the bundled `serve-openlinker-agent` skill and local Agent-control tools.
Agent Mode is disabled by default. Never accept Agent Tokens, API keys, secret
file contents, or Provider session IDs in this command. Diagnose first and
enable or disable only when the user's request is explicit.

User request: $ARGUMENTS
